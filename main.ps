Import-Csv C:\AP.csv | Foreach-Object
 {
     $DeviceName = $_."Name"
     $IPAddress = $_."IP"
     $SecretPass = $_."SecretKey"
     New-NpsRadiusClient -Address $IPAddress -Name $DeviceName -SharedSecret $SecretPass
 }
